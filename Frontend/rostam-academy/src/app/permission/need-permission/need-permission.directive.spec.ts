import { NeedPermissionDirective } from "./NeedPermissionDirective";

describe('NeedPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new NeedPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
